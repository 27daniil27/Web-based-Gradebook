import { Button } from "@/components/ui/button";

function LoginPage() {
    return (
        <div>
            <h1>Login Page</h1>
            <Button variant="default">Login</Button>
        </div>
    );
}

export default LoginPage;
